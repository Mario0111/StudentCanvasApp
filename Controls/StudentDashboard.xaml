<UserControl x:Class="StudentCanvasApp.Controls.StudentDashboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="400" Width="400">
    <StackPanel Margin="20">
        <TextBlock Text="Student Dashboard" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" HorizontalAlignment="Center"/>

        <TextBlock x:Name="WelcomeText" FontWeight="Bold" Margin="0,0,0,10"/>

        <ListBox x:Name="ClassListBox" Height="150" SelectionChanged="ClassListBox_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding ClassName}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock Text="Assignments:" FontWeight="Bold" Margin="0,10,0,5"/>
        <ListBox x:Name="AssignmentListBox" Height="150">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Title}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>


        <Button Content="Logout" Margin="0,20,0,0" Click="Logout_Click"/>
    </StackPanel>
</UserControl>
